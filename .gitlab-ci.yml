before_script:
  - apt-get update -qq && apt-get install -y -qq curl
  - curl -fsSL https://get.docker.com/ | sh
  - docker -v

build_master:
  script:
    - docker build -t registry.gitlab.com/Sraleik/docker_ubuntu:16.04-blank .
